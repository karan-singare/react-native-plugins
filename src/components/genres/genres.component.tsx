import React from 'react';
import { View, Text } from 'react-native';

export default class Genres extends React.Component {
  constructor(props: any) {
    super(props);
  }

  render() {
    return (
      <View>
        <Text>This is genre plugin component</Text>
      </View>
    );
  }
}
